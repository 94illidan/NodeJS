/*
    Vamos con otro ejercicio de los básicos. Buscar maximos y minimos y buscar elementos en particular.
*/

// ************** No le de importancia a esta porción de código ************** //
// Vamos a generar un array de forma aleatoria.
let elArray = [];
let cantidadArray = Math.floor(Math.random() * Math.floor(15)) + 3;

for(let i = 0; i <= cantidadArray; i++){
    elArray[i] = Math.floor(Math.random() * Math.floor(10)) ;
}
// *************************************************************************** //


// A continuacion las funciones de máximos, minimos y devolver posicion que deberá implementar.

// Devuelve el número más grande de un array.
function maximo(arrayParaSumar){

    // Implemente aqui el código de la función.
    // Recuerde que para hacer referencia a una posicion del array deberá usar: arrayParaSumar[<numero de la posicion>]. Ej: arrayParaSumar[3]


    // Devuelva el resultado
    return ;

}

// Devuelve el número más chico de un array
function minimo(arrayParaSumar){

    // Implemente aqui el código de la función.
    // Recuerde que para hacer referencia a una posicion del array deberá usar: arrayParaSumar[<numero de la posicion>]. Ej: arrayParaSumar[3]


    // Devuelva el resultado
    return ;

}

// Se anima a llamar a las funciones máximo y minimo?
// Recuerde que el array que estamos usando para enviar a la funcion es: elArray


// -------------------------------------------------------------------------------------------------------- //
// Y si quisieramos saner en que posicion se encuentra la primera ocurrencia de un elemento? Como lo haríamos?
// Quiero que al pasarle como parámetro un array y un número me devuelva la posicion en la que se ubica el preimer elemento o un -1 si no existe.
// Recuerde que la funcion recibe dos parámetros como lo hicimos con la suma y devuelve siempre un numero.

// Funcion:


// Llamada a la funcion:
// console.log();

