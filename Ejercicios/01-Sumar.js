/*
    Vamos a comenzar con la diversion de programar!
    En este primer ejercico, lo basico.
    Si lo ejecuta vera que devuelve u undefined, deberá implementar la funcion SUMAR para obtener un resultado.
*/

// ************** No le de importancia a esta porción de código ************** //
// Vamos a generar numero aletorios.
const numeroUno = Math.floor(Math.random() * Math.floor(10));
const numeroDos = Math.floor(Math.random() * Math.floor(10));
// *************************************************************************** //

// Implemente la siguiente función que devuelva la suma de ambos números enviados como parámetro.
function sumar(numA, numB){

    // Implemente aqui el código de la función.


    // Devuelva el resultado
    return ;

}

// Llamemos a la funcion pasándole como parámetro el array que acabamos de crear:
console.log(sumar(numeroUno, numeroDos));

// ---------------------------------------------------------------------------------------------------- //

// Como reescribiria el console.log para saber que números está sumando y si está bien el resultado.
// O sea, queremos algo como: "La suma de: 3 más 4 es igual a 7"

// console.log(/* Su código aqui. */);